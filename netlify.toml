[build]
publish = "dist"
command = "npx pnpm@10.12.4 install --frozen-lockfile && npx pnpm@10.12.4 run build"

[build.environment]
NPM_FLAGS = "--version"
NODE_VERSION = "22.13.0"
COREPACK_ENABLE_DOWNLOAD_PROMPT = "0"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
